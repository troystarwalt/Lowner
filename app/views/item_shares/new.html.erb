<h1>ItemShares#new</h1>
<p>Find me in app/views/item_shares/new.html.erb</p>


<!-- User comes here from their profile after clicking on Loan next to the item they want to loan.
Ideally, thee Iten.id will follow the path and will be available on the next screen.
They then need to select a user to share this item with. So that the ItemShare will include the User.id of the other user as well as the current_user's Item.id.

 -->

 <p>Who do you want to share this with?</p>

<h3>A list of all the Users</h3>
<%= form_tag new_item_share_path, :method => 'get', id: 'users_search' do %>
<p>
	<%= text_field_tag :search_users, params[:search_users] %>
	<%= hidden_field_tag :item_id, params[:item_id] %>
	<%= submit_tag "Search", :name => nil %>
</p>
<% end %>
<%  @users.each do |user| %>

	<%= user.username %>
	<%= user.id %>
	<%= link_to "Loan It", item_shares_path(:user_id => user.id, :item_id => @item_id), :method => :post %>
	<hr>
	<br>

<% end %>	